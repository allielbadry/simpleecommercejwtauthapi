const productController = require("../controllers/productController");
const router = require("express").Router();

// router.post("/:catId/newproduct", productController.createProduct);

module.exports = router;
